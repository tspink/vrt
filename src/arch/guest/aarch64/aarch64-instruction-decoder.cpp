#include <arch/guest/aarch64/aarch64-instruction-decoder.h>
#include <arch/guest/aarch64/aarch64-instruction.h>

using namespace vrt::arch;
using namespace vrt::arch::guest;
using namespace vrt::arch::guest::aarch64;

Instruction* AArch64InstructionDecoder::decode(virt_addr_t va)
{
	return nullptr;
}
